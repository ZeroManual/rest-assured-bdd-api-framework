base.uri=https://jsonplaceholder.typicode.com
request.timeout.ms=10000
response.timeout.ms=10000

# Optional auth values. In real projects, pass these from Jenkins credentials, not Git.
auth.token=
api.key=
api.key.header=x-api-key

# DB validation. Keep disabled unless DB details are supplied through Jenkins secrets.
db.enabled=false
db.url=jdbc:postgresql://host:5432/db
db.username=
db.password=
